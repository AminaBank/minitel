{
  "name": "protocols-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "installConfig": {
    "hoisting": false
  },
  "scripts": {
    "format": "bun --filter './apps/ethereum' --filter './apps/solana' format",
    "lint": "bun --filter './apps/ethereum' --filter './apps/solana' lint",
    "dev": "bun --filter './apps/ethereum' --filter './apps/solana' dev",
    "build": "bun --filter './apps/ethereum' --filter './apps/solana' build",
    "clean": "rm -rf node_modules packages/*/node_modules apps/*/node_modules"
  },
  "dependencies": {
    "lucide-react": "^0.544.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}